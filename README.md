# tg_bot_template

Минимальный каркас Telegram-бота с inline-меню, SQLite и настройками из `.env`.

## Быстрый старт (Windows + uv)
1) Скопировать `.env.example` → `.env` и заполнить значения.
2) Создать и активировать виртуальное окружение:
   - `uv venv .venv`
   - `.venv\Scripts\activate`
3) Установить зависимости:
   - `uv pip install -e ".[dev]"`
4) Запуск:
   - `python -m app.main ping --dry-run`
   - `python -m app.main bot`

## Где править
- Меню и навигация: `app/bot/handlers_accounts.py`, `app/bot/keyboards.py`
- Настройки: `app/settings.py`
- База: `app/db/repo.py`
- Модули: `app/modules/*`

## Как добавить модуль
1) Создайте класс модуля, унаследованный от `BaseModule`.
2) Добавьте инстанс в `ENABLED_MODULES` в `app/modules/registry.py`.
3) Перезапустите бота — модуль появится в меню “🧩 Модули”.

## Примечания
- По умолчанию `DRY_RUN=true`.
- Таблица `meta` создаётся при старте бота.
